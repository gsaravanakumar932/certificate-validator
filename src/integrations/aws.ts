// AWS SDK v3 clients (stubs) for cloud needs
/*
import { S3Client } from '@aws-sdk/client-s3';
import { SecretsManagerClient } from '@aws-sdk/client-secrets-manager';

export const s3 = new S3Client({ region: process.env.AWS_REGION || 'us-east-1' });
export const secretsManager = new SecretsManagerClient({ region: process.env.AWS_REGION || 'us-east-1' });
*/

export {};
